version: "3"
services:
  code2vec:
    build:
      context: .
      dockerfile: Dockerfile
    user: "${C2VUID}:${C2VGID}"
    container_name: code2vec
    volumes:
      - ./:/code2vec/
      - /mnt/data1/tomohiro-m/Sample1:/data
    # runtime: nvidia
    # environment:
    #   - NVIDIA_VISIBLE_DEVICES=all
    stdin_open: true
    
    working_dir: /code2vec
    
