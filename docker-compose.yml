version: "3"
services:
  code2vec:
    build:
      context: .
      dockerfile: Dockerfile
    user: "${C2VUID}:${C2VGID}"
    container_name: code2vec-m
    volumes:
      - ./:/code2vec/
      - tomohiro-m@133.42.160.101:/mnt/data1/tomohiro-m/Sample1:/data
    # runtime: nvidia
    # environment:
    #   - NVIDIA_VISIBLE_DEVICES=all
    stdin_open: true
    
    working_dir: /code2vec
    
